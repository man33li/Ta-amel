---
phase: 04-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - .env.example
  - README.md
autonomous: true

must_haves:
  truths:
    - "npm run build completes with zero errors"
    - "Environment variables are documented with sourcing instructions"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Dynamic rendering export to prevent prerender failure"
      contains: "export const dynamic"
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Dynamic rendering export to prevent prerender failure"
      contains: "export const dynamic"
    - path: ".env.example"
      provides: "Template with all required env vars and sourcing comments"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
    - path: "README.md"
      provides: "Deployment documentation with env var setup"
      contains: "Environment Variables"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "Next.js build"
      via: "dynamic export prevents static prerender"
      pattern: "export const dynamic"
---

<objective>
Fix production build errors and document environment variables.

Purpose: DEP-01 (clean build) and DEP-02 (env docs) are prerequisites for deployment.
Output: Zero-error build + documented env vars in .env.example and README.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Known blocker from STATE.md: Build prerender fails on /login and /signup because
Supabase client is created at build time without valid env vars. Auth pages are
'use client' but Next.js still attempts prerendering.

Current .env.local has: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY
Current .env.example exists but only has NEXT_PUBLIC_SUPABASE_URL placeholder.
README is default create-next-app boilerplate.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix build prerender failures on auth pages</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/signup/page.tsx</files>
  <action>
Add `export const dynamic = 'force-dynamic'` to both auth page files.
Place the export BEFORE the default export, after imports. This tells Next.js
to skip static prerendering for these pages since they depend on Supabase client
which requires runtime env vars.

Do NOT create a shared auth layout with the dynamic export — each page should
have its own export so the intent is explicit and pages remain independent.

After adding the exports, run `npm run build` and verify it completes with zero errors.
If there are any other build errors or warnings (besides the middleware deprecation notice
which is a Next.js 16 informational warning), fix them.
  </action>
  <verify>Run `npm run build` — must complete successfully with exit code 0. The only acceptable warning is the Next.js middleware deprecation notice.</verify>
  <done>Build completes with zero errors. Auth pages render dynamically at request time instead of failing at build time.</done>
</task>

<task type="auto">
  <name>Task 2: Document environment variables and deployment</name>
  <files>.env.example, README.md</files>
  <action>
Update `.env.example` to include both required vars with sourcing instructions:

```
# Supabase Configuration
# Get these values from your Supabase project dashboard:
# https://supabase.com/dashboard/project/_/settings/api

NEXT_PUBLIC_SUPABASE_URL=your-project-url-here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

Replace the entire README.md with project-specific content:

1. **MindForge** header with one-line description (note-taking app built with Next.js and Supabase)
2. **Tech Stack** section: Next.js 16, Supabase (auth + database), Zustand, Tiptap, Tailwind CSS
3. **Getting Started** section: clone, install, env setup (reference .env.example), run dev
4. **Environment Variables** section: table with var name, description, where to get it
5. **Deployment** section: Vercel-specific instructions — connect repo, set env vars in Vercel dashboard, deploy. No generic self-host instructions.
6. **Scripts** section: dev, build, start, test, test:coverage, lint

Keep it concise — no boilerplate filler. Each section should be 3-8 lines max.
  </action>
  <verify>Verify .env.example has both NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY with comments. Verify README.md has Environment Variables and Deployment sections.</verify>
  <done>.env.example documents all required vars with sourcing instructions. README includes environment setup and Vercel deployment guide.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits with code 0
2. `.env.example` has both Supabase vars with comments
3. `README.md` has Environment Variables table and Deployment section
4. `npm test` still passes (no regressions from dynamic export)
</verification>

<success_criteria>
- Build completes cleanly (DEP-01 satisfied)
- Env vars fully documented (DEP-02 satisfied)
- Tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment/04-01-SUMMARY.md`
</output>
