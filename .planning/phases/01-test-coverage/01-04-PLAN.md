---
phase: 01-test-coverage
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/__tests__/pages/Signup.test.tsx
autonomous: true

must_haves:
  truths:
    - "Signup form validates password match"
    - "Signup form validates password length"
    - "Signup calls signUp with email and password"
    - "Signup shows success message after registration"
    - "Signup shows error message on failure"
  artifacts:
    - path: "src/__tests__/pages/Signup.test.tsx"
      provides: "Signup page integration tests"
      min_lines: 100
  key_links:
    - from: "src/__tests__/pages/Signup.test.tsx"
      to: "src/lib/supabase/client.ts"
      via: "mock signUp"
      pattern: "signUp.*mockResolvedValue"
---

<objective>
Test auth integration: Signup page.

Purpose: Covers TEST-03 (Auth flows have integration tests). Login is already tested. Signup has unique validation (password match, confirmation) that needs coverage.

Output: Comprehensive test file for Signup page.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-test-coverage/01-RESEARCH.md
@src/__tests__/setup.ts
@src/__tests__/pages/Login.test.tsx
@src/app/(auth)/signup/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Signup page integration tests</name>
  <files>src/__tests__/pages/Signup.test.tsx</files>
  <action>
Create comprehensive tests for Signup page, following Login.test.tsx patterns:

1. **Rendering tests:**
   - Renders email, password, and confirm password inputs
   - Renders signup button
   - Renders Google OAuth button
   - Renders link to login page

2. **Validation tests:**
   - Shows error when passwords don't match
   - Shows error when password is less than 6 characters

3. **Form submission tests:**
   - Calls signUp with email and password on valid submit
   - Shows loading state during submission
   - Shows success message after successful signup (check email)
   - Shows error message on signup failure

4. **Google OAuth:**
   - Calls signInWithOAuth when Google button clicked

Pattern (mirror Login.test.tsx):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import SignupPage from '@/app/(auth)/signup/page'

const mockSignUp = vi.fn()
const mockSignInWithOAuth = vi.fn()

vi.mock('@/lib/supabase/client', () => ({
  createClient: () => ({
    auth: {
      signUp: mockSignUp,
      signInWithOAuth: mockSignInWithOAuth,
    },
  }),
}))

describe('SignupPage', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  describe('Validation', () => {
    it('shows error when passwords do not match', async () => {
      const user = userEvent.setup()
      render(<SignupPage />)
      
      await user.type(screen.getByPlaceholderText(/email/i), 'test@example.com')
      await user.type(screen.getByPlaceholderText('Password'), 'password123')
      await user.type(screen.getByPlaceholderText(/confirm/i), 'different')
      await user.click(screen.getByRole('button', { name: /sign up/i }))
      
      expect(screen.getByText(/passwords do not match/i)).toBeInTheDocument()
      expect(mockSignUp).not.toHaveBeenCalled()
    })
  })

  describe('Successful Signup', () => {
    it('shows success message after signup', async () => {
      const user = userEvent.setup()
      mockSignUp.mockResolvedValue({ error: null })
      render(<SignupPage />)
      
      await user.type(screen.getByPlaceholderText(/email/i), 'test@example.com')
      await user.type(screen.getByPlaceholderText('Password'), 'password123')
      await user.type(screen.getByPlaceholderText(/confirm/i), 'password123')
      await user.click(screen.getByRole('button', { name: /sign up/i }))
      
      await waitFor(() => {
        expect(screen.getByText(/check your email/i)).toBeInTheDocument()
      })
    })
  })
})
```
  </action>
  <verify>Run `npm run test:run -- src/__tests__/pages/Signup.test.tsx` - all tests pass</verify>
  <done>Signup page has 8+ tests covering render, validation, success, and error states</done>
</task>

<task type="auto">
  <name>Task 2: Verify auth tests run together</name>
  <files>-</files>
  <action>
Run both Login and Signup tests together to ensure no conflicts:

```bash
npm run test:run -- src/__tests__/pages/
```

Verify:
1. Both test files are discovered
2. All tests pass
3. No mock conflicts between files
  </action>
  <verify>Run `npm run test:run -- src/__tests__/pages/` - all tests pass</verify>
  <done>Both Login and Signup tests pass together</done>
</task>

</tasks>

<verification>
- [ ] `src/__tests__/pages/Signup.test.tsx` exists with 8+ tests
- [ ] Tests cover: render, validation, success state, error state, OAuth
- [ ] `npm run test:run -- src/__tests__/pages/` passes with 0 failures
</verification>

<success_criteria>
1. Signup page has comprehensive tests matching Login.test.tsx quality
2. Validation edge cases covered (password match, length)
3. Success and error states tested
4. All auth tests pass together
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-coverage/01-04-SUMMARY.md`
</output>
