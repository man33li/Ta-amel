---
phase: 01-test-coverage
plan: 06
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All tests pass when run together"
    - "Coverage report shows component coverage"
    - "Coverage report shows hook coverage"
    - "No test failures or skipped tests (except documented)"
  artifacts:
    - path: "coverage/"
      provides: "V8 coverage report"
  key_links: []
---

<objective>
Run final test suite and generate coverage report.

Purpose: Verify TEST-05 (coverage report available) and validate that all prior plans achieved TEST-01 through TEST-04. This is the verification checkpoint for the entire phase.

Output: Coverage report generated, all tests pass.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files>-</files>
  <action>
Run the complete test suite to verify all tests pass:

```bash
npm run test:run
```

Expected:
- All test files discovered (stores/, components/, hooks/, pages/)
- All tests pass (0 failures)
- No unexpected errors

If any tests fail:
1. Check error message
2. Fix the failing test
3. Re-run until all pass
  </action>
  <verify>Run `npm run test:run` - exits with code 0, shows all tests passing</verify>
  <done>Full test suite passes with 0 failures</done>
</task>

<task type="auto">
  <name>Task 2: Generate coverage report</name>
  <files>-</files>
  <action>
Generate coverage report:

```bash
npm run test:coverage
```

This runs tests with v8 coverage provider and generates:
- `coverage/` directory with HTML report
- Console output with coverage summary

Capture the output to review coverage percentages.
  </action>
  <verify>Run `npm run test:coverage` - completes successfully, coverage/ directory created</verify>
  <done>Coverage report generated in coverage/ directory</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete test suite with coverage report</what-built>
  <how-to-verify>
Review the coverage output:

1. Check console summary for coverage percentages
2. Open coverage/index.html in browser for detailed report
3. Verify these areas have coverage:
   - Components: ThemeProvider, ToastProvider, ThemeToggle, Header, NoteCard, TiptapEditor
   - Hooks: useNotes
   - Stores: useAppStore
   - Pages: Login, Signup, Dashboard

4. Note any gaps in coverage for future improvement

5. Confirm requirements met:
   - TEST-01: Components have coverage ✓/✗
   - TEST-02: Hooks have coverage ✓/✗
   - TEST-03: Auth flows tested ✓/✗
   - TEST-04: Note CRUD tested ✓/✗
   - TEST-05: Coverage script works ✓/✗
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe any gaps</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run test:run` passes with 0 failures
- [ ] `npm run test:coverage` generates coverage/ directory
- [ ] Coverage report shows component files tested
- [ ] Coverage report shows hook files tested
- [ ] User approves coverage levels
</verification>

<success_criteria>
1. All tests pass
2. Coverage report generated
3. User confirms requirements TEST-01 through TEST-05 are met
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-coverage/01-06-SUMMARY.md`
</output>
