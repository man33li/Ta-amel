---
phase: 01-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/stores/useAppStore.test.ts
  - src/__tests__/components/ThemeProvider.test.tsx
  - src/__tests__/components/ToastProvider.test.tsx
autonomous: true

must_haves:
  truths:
    - "useAppStore can toggle theme between light and dark"
    - "useAppStore persists theme to localStorage"
    - "ThemeProvider applies dark class to document when theme is dark"
    - "ToastProvider renders without crashing"
  artifacts:
    - path: "src/__tests__/stores/useAppStore.test.ts"
      provides: "Zustand store unit tests"
      min_lines: 60
    - path: "src/__tests__/components/ThemeProvider.test.tsx"
      provides: "Theme provider integration tests"
      min_lines: 30
    - path: "src/__tests__/components/ToastProvider.test.tsx"
      provides: "Toast provider render test"
      min_lines: 15
  key_links:
    - from: "src/__tests__/stores/useAppStore.test.ts"
      to: "src/stores/useAppStore.ts"
      via: "direct import"
      pattern: "import.*useAppStore"
---

<objective>
Test the foundation layer: Zustand store and provider components.

Purpose: These are the base layers that other components depend on. Testing them first validates the core state management before testing UI components that consume this state.

Output: Three test files covering useAppStore, ThemeProvider, and ToastProvider.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-test-coverage/01-RESEARCH.md
@src/__tests__/setup.ts
@src/stores/useAppStore.ts
@src/components/providers/ThemeProvider.tsx
@src/components/providers/ToastProvider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useAppStore unit tests</name>
  <files>src/__tests__/stores/useAppStore.test.ts</files>
  <action>
Create comprehensive unit tests for the Zustand store:

1. Create directory: `src/__tests__/stores/`
2. Create `useAppStore.test.ts` with tests for:
   - Initial state values (theme: 'light', accentColor, activeNoteId, sidebarOpen)
   - `setTheme()` action changes theme
   - `toggleTheme()` action toggles between light/dark
   - `setAccentColor()` action updates accent color
   - `setActiveNoteId()` action updates active note
   - `setSidebarOpen()` / `toggleSidebar()` actions work
   - `useTheme()` and `useToggleTheme()` selector hooks work

Testing Zustand pattern:
```typescript
import { useAppStore } from '@/stores/useAppStore'
import { act } from '@testing-library/react'

// Reset store between tests
beforeEach(() => {
  useAppStore.setState({
    theme: 'light',
    accentColor: '#3b82f6',
    activeNoteId: null,
    sidebarOpen: true
  })
})
```

Mock localStorage to avoid persistence issues in tests:
```typescript
const localStorageMock = { getItem: vi.fn(), setItem: vi.fn(), removeItem: vi.fn() }
Object.defineProperty(window, 'localStorage', { value: localStorageMock })
```
  </action>
  <verify>Run `npm run test:run -- src/__tests__/stores/useAppStore.test.ts` - all tests pass</verify>
  <done>useAppStore has 7+ tests covering all actions and selectors</done>
</task>

<task type="auto">
  <name>Task 2: Create ThemeProvider and ToastProvider tests</name>
  <files>src/__tests__/components/ThemeProvider.test.tsx, src/__tests__/components/ToastProvider.test.tsx</files>
  <action>
Create tests for provider components:

**ThemeProvider.test.tsx:**
1. Test that it renders children
2. Test that dark class is added to document.documentElement when theme is 'dark'
3. Test that dark class is removed when theme is 'light'

Pattern:
```typescript
import { render } from '@testing-library/react'
import { ThemeProvider } from '@/components/providers/ThemeProvider'
import { useAppStore } from '@/stores/useAppStore'

beforeEach(() => {
  document.documentElement.classList.remove('dark')
  useAppStore.setState({ theme: 'light' })
})

it('applies dark class when theme is dark', () => {
  useAppStore.setState({ theme: 'dark' })
  render(<ThemeProvider><div>child</div></ThemeProvider>)
  expect(document.documentElement.classList.contains('dark')).toBe(true)
})
```

**ToastProvider.test.tsx:**
1. Test that it renders without crashing (smoke test)
2. This is a thin wrapper around react-hot-toast, so minimal testing needed

```typescript
import { render } from '@testing-library/react'
import { ToastProvider } from '@/components/providers/ToastProvider'

it('renders without crashing', () => {
  expect(() => render(<ToastProvider />)).not.toThrow()
})
```
  </action>
  <verify>Run `npm run test:run -- src/__tests__/components/ThemeProvider.test.tsx src/__tests__/components/ToastProvider.test.tsx` - all tests pass</verify>
  <done>Both provider components have tests, ThemeProvider tests document class manipulation</done>
</task>

<task type="auto">
  <name>Task 3: Verify all foundation tests pass together</name>
  <files>-</files>
  <action>
Run all tests in the foundation layer to ensure they work together:

1. Run: `npm run test:run`
2. Verify no test failures
3. Check that new tests are discovered (should see stores/ and updated components/)

If any tests fail, fix them before marking complete.
  </action>
  <verify>Run `npm run test:run` - 0 failures, new tests appear in output</verify>
  <done>All existing + new tests pass together</done>
</task>

</tasks>

<verification>
- [ ] `src/__tests__/stores/useAppStore.test.ts` exists with 7+ tests
- [ ] `src/__tests__/components/ThemeProvider.test.tsx` exists with 2+ tests  
- [ ] `src/__tests__/components/ToastProvider.test.tsx` exists with 1+ test
- [ ] `npm run test:run` passes with 0 failures
</verification>

<success_criteria>
1. useAppStore fully tested (all actions, selectors)
2. ThemeProvider tests document.documentElement class manipulation
3. ToastProvider has smoke test
4. All tests pass when run together
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-coverage/01-01-SUMMARY.md`
</output>
