---
phase: 01-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/components/ThemeToggle.test.tsx
  - src/__tests__/components/Header.test.tsx
autonomous: true

must_haves:
  truths:
    - "ThemeToggle button toggles theme when clicked"
    - "ThemeToggle shows correct icon based on current theme"
    - "Header renders logo link to home"
    - "Header contains ThemeToggle component"
  artifacts:
    - path: "src/__tests__/components/ThemeToggle.test.tsx"
      provides: "Theme toggle button tests"
      min_lines: 40
    - path: "src/__tests__/components/Header.test.tsx"
      provides: "Header component tests"
      min_lines: 25
  key_links:
    - from: "src/__tests__/components/ThemeToggle.test.tsx"
      to: "src/stores/useAppStore.ts"
      via: "store mock or direct state"
      pattern: "useAppStore"
---

<objective>
Test UI components: ThemeToggle and Header.

Purpose: These are user-facing UI components that need interaction testing. ThemeToggle tests state changes, Header tests structure and navigation.

Output: Two test files for ThemeToggle and Header components.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-test-coverage/01-RESEARCH.md
@src/__tests__/setup.ts
@src/components/ui/ThemeToggle.tsx
@src/components/ui/Header.tsx
@src/stores/useAppStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ThemeToggle component tests</name>
  <files>src/__tests__/components/ThemeToggle.test.tsx</files>
  <action>
Create comprehensive tests for ThemeToggle:

1. Test rendering:
   - Renders a button element
   - Has appropriate aria-label for accessibility

2. Test interaction:
   - Clicking toggles theme from light to dark
   - Clicking again toggles back to light

3. Test visual state:
   - Shows moon icon when theme is light (switch to dark)
   - Shows sun icon when theme is dark (switch to light)

Pattern:
```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { ThemeToggle } from '@/components/ui/ThemeToggle'
import { useAppStore } from '@/stores/useAppStore'

beforeEach(() => {
  useAppStore.setState({ theme: 'light' })
})

it('toggles theme when clicked', async () => {
  const user = userEvent.setup()
  render(<ThemeToggle />)
  
  expect(useAppStore.getState().theme).toBe('light')
  await user.click(screen.getByRole('button'))
  expect(useAppStore.getState().theme).toBe('dark')
})

it('shows correct aria-label based on theme', () => {
  render(<ThemeToggle />)
  expect(screen.getByRole('button')).toHaveAttribute(
    'aria-label', 
    'Switch to dark mode'
  )
})
```
  </action>
  <verify>Run `npm run test:run -- src/__tests__/components/ThemeToggle.test.tsx` - all tests pass</verify>
  <done>ThemeToggle has 4+ tests covering render, interaction, and accessibility</done>
</task>

<task type="auto">
  <name>Task 2: Create Header component tests</name>
  <files>src/__tests__/components/Header.test.tsx</files>
  <action>
Create tests for Header component:

1. Test structure:
   - Renders header element
   - Contains link to home page with "MindForge" text
   - Contains ThemeToggle component

2. Test navigation:
   - Logo link has correct href="/"

Pattern:
```typescript
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { Header } from '@/components/ui/Header'

it('renders logo link to home', () => {
  render(<Header />)
  
  const logoLink = screen.getByRole('link', { name: /mindforge/i })
  expect(logoLink).toHaveAttribute('href', '/')
})

it('contains theme toggle button', () => {
  render(<Header />)
  
  // ThemeToggle renders a button with aria-label
  expect(screen.getByRole('button', { name: /switch to/i })).toBeInTheDocument()
})

it('renders as header element', () => {
  render(<Header />)
  
  expect(screen.getByRole('banner')).toBeInTheDocument()
})
```
  </action>
  <verify>Run `npm run test:run -- src/__tests__/components/Header.test.tsx` - all tests pass</verify>
  <done>Header has 3+ tests covering structure and navigation</done>
</task>

</tasks>

<verification>
- [ ] `src/__tests__/components/ThemeToggle.test.tsx` exists with 4+ tests
- [ ] `src/__tests__/components/Header.test.tsx` exists with 3+ tests
- [ ] `npm run test:run` passes with 0 failures
</verification>

<success_criteria>
1. ThemeToggle tests cover toggle functionality and accessibility
2. Header tests verify structure and navigation
3. All tests pass when run together
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-coverage/01-02-SUMMARY.md`
</output>
