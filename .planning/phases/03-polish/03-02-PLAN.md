---
phase: 03-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/notes/NoteCard.tsx
  - src/components/notes/TiptapEditor.tsx
  - src/app/(main)/page.tsx
autonomous: true

must_haves:
  truths:
    - "All interactive elements have visible focus indicators when tabbed to"
    - "NoteCard shows focus ring when navigated via keyboard"
    - "Toolbar buttons have visible focus state"
    - "Empty state is visually clear and actionable"
  artifacts:
    - path: "src/components/notes/NoteCard.tsx"
      provides: "Note card with focus-visible styling"
      contains: "focus-visible"
    - path: "src/components/notes/TiptapEditor.tsx"
      provides: "Toolbar buttons with focus-visible styling"
      contains: "focus-visible"
  key_links:
    - from: "NoteCard"
      to: "keyboard navigation"
      via: "focus-visible:ring class"
      pattern: "focus-visible:ring"
---

<objective>
Add keyboard accessibility and polish edge case handling

Purpose: Complete POL-03 (edge cases) and POL-04 (keyboard navigation) requirements
Output: Enhanced focus states on all interactive elements, polished empty states
</objective>

<execution_context>
@C:\Users\Admin\.config\opencode/get-shit-done/workflows/execute-plan.md
@C:\Users\Admin\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

Key patterns:
- Tailwind CSS with focus-visible: prefix for keyboard-only focus
- Existing focus pattern on auth inputs: `focus:ring-2 focus:ring-blue-500`
- NoteCard uses Link component (focusable by default)
- Empty state already exists on home page - may need minor polish
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focus-visible styles to NoteCard</name>
  <files>src/components/notes/NoteCard.tsx</files>
  <action>
Enhance the NoteCard Link component to have visible focus indicators for keyboard navigation.

Current className (lines 23-25):
```
"block p-4 border border-gray-200 dark:border-gray-700 rounded-lg 
 hover:shadow-md hover:border-gray-300 dark:hover:border-gray-600
 bg-white dark:bg-gray-800 transition-all duration-200"
```

Add focus-visible classes:
```
focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 
focus-visible:ring-offset-2 dark:focus-visible:ring-offset-gray-900
```

This ensures:
- No outline on click (focus-visible, not focus)
- Blue ring when tabbed to (matches app accent color)
- Ring offset for visual separation
- Dark mode offset color adjustment
  </action>
  <verify>Tab through notes grid, see blue ring around focused NoteCard, click doesn't show ring.</verify>
  <done>NoteCard shows clear focus ring when navigated via keyboard Tab</done>
</task>

<task type="auto">
  <name>Task 2: Add focus-visible styles to TiptapEditor toolbar buttons</name>
  <files>src/components/notes/TiptapEditor.tsx</files>
  <action>
Enhance the ToolbarButton component to have visible focus indicators.

Current className in ToolbarButton (lines 200-206):
```tsx
className={`
  px-2 py-1.5 rounded text-sm font-medium transition-colors
  ${active 
    ? 'bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300' 
    : 'text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700'
  }
`}
```

Add focus-visible classes (append to the static part):
```
focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-inset
```

Use `ring-inset` because buttons are small and ring-offset would look too large.

Also ensure buttons have accessible name (they have title attribute already ✓).
  </action>
  <verify>Tab into editor toolbar, see focus ring on each button, Escape returns to editor.</verify>
  <done>Toolbar buttons show focus ring when navigated via keyboard</done>
</task>

<task type="auto">
  <name>Task 3: Verify and confirm edge case handling (POL-03)</name>
  <files>src/app/(main)/page.tsx, src/components/notes/NoteCard.tsx</files>
  <action>
Verify that existing edge case handling meets POL-03 requirements.

**Empty state verification:**
The home page already has an empty state (lines 75-90) that shows:
- "No notes yet" heading
- "Create your first note to get started" message
- "Create First Note" button

Verify this works by checking code structure. No code changes needed if complete.

**Long content truncation verification:**
NoteCard already handles long content (lines 27-33):
- Title uses `truncate` class (single line with ellipsis)
- Preview uses `line-clamp-2` class (2 lines with ellipsis)
- Preview capped at 200 chars in extractTextFromContent

Verify this works by checking code structure. No code changes needed if complete.

**If any gaps found:**
- Empty state: Add emoji (📝) icon, improve CTA styling
- Long content: Ensure truncate classes are present

The existing implementation should satisfy POL-03. This task confirms it.
  </action>
  <verify>
1. Check home page code: empty state exists with heading, description, CTA button
2. Check NoteCard code: truncate on title, line-clamp-2 on preview
3. Run `npm run build` to confirm no errors
  </verify>
  <done>Edge cases confirmed: empty state shows helpful prompt, long content truncates gracefully</done>
</task>

</tasks>

<verification>
1. `npm run lint` passes
2. `npm run build` passes
3. Manual keyboard navigation test:
   - Tab from address bar into page
   - Tab highlights header elements
   - Tab highlights NoteCards with visible ring
   - Tab into note editor, toolbar buttons focusable
   - All focus states are visible (blue ring)
   - Click on elements does NOT show focus ring (focus-visible)
</verification>

<success_criteria>
- POL-03: Edge cases handled gracefully
  - Empty state: verified in home page (already exists with heading, prompt, CTA button)
  - Long content: verified in NoteCard (truncate class on title, line-clamp-2 on preview)
- POL-04: All interactive elements reachable and operable via keyboard with visible focus
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish/03-02-SUMMARY.md`
</output>
