---
phase: 03-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Spinner.tsx
  - src/app/(main)/page.tsx
  - src/app/(main)/notes/[id]/page.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
autonomous: true

must_haves:
  truths:
    - "Loading spinner appears on auth buttons during submission"
    - "Loading skeleton appears during notes fetch (already exists, verified)"
    - "Error states show retry button that reloads data"
    - "Clicking retry re-fetches notes and clears error"
  artifacts:
    - path: "src/components/ui/Spinner.tsx"
      provides: "Reusable loading spinner component"
      exports: ["Spinner"]
    - path: "src/app/(main)/page.tsx"
      provides: "Home page with error retry"
      contains: "refetch"
  key_links:
    - from: "src/app/(main)/page.tsx"
      to: "useNotes.refetch"
      via: "retry button onClick"
      pattern: "onClick.*refetch"
---

<objective>
Add loading spinners and error retry functionality

Purpose: Complete POL-01 (loading states) and POL-02 (error retry) requirements
Output: Spinner component, enhanced error states with retry buttons on all async pages
</objective>

<execution_context>
@C:\Users\Admin\.config\opencode/get-shit-done/workflows/execute-plan.md
@C:\Users\Admin\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

Key patterns from codebase:
- Tailwind CSS for styling, `animate-` classes for animations
- Components in src/components/{category}/
- useNotes hook already exposes `refetch` function
- Error states use red bg/border pattern consistently
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Spinner component</name>
  <files>src/components/ui/Spinner.tsx</files>
  <action>
Create a simple, reusable Spinner component using Tailwind CSS.

```tsx
interface SpinnerProps {
  size?: 'sm' | 'md' | 'lg'
  className?: string
}
```

Implementation:
- Use `animate-spin` class for rotation animation
- SVG circle with stroke-dasharray for the spinner look
- Three size variants: sm (w-4 h-4), md (w-5 h-5), lg (w-6 h-6)
- Accept className prop for color customization
- Default color: currentColor (inherits from parent)
- Export as named export `Spinner`

Follow existing component patterns: PascalCase file, named export, Props interface.
  </action>
  <verify>File exists and exports Spinner. TypeScript compiles without errors.</verify>
  <done>Spinner component created with size variants and customizable className</done>
</task>

<task type="auto">
  <name>Task 2: Add error retry to home page</name>
  <files>src/app/(main)/page.tsx</files>
  <action>
Enhance the error state in HomePage to include a retry button.

Current error state (lines 47-56) shows error message but no retry option.

Changes:
1. Import `refetch` from useNotes (already destructured on line 18, need to add it)
2. Update error state JSX to include a "Try Again" button
3. Button should call `refetch()` onClick
4. Style button consistently: `px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg`
5. Add `mt-4` margin between error message and button

The useNotes hook already exposes refetch, so this is just wiring.
  </action>
  <verify>Visit home page, simulate error (disconnect network), see retry button, click to reload.</verify>
  <done>Error state shows "Try Again" button that calls refetch on click</done>
</task>

<task type="auto">
  <name>Task 3: Add loading spinners to auth buttons and retry to note editor</name>
  <files>
    src/app/(auth)/login/page.tsx
    src/app/(auth)/signup/page.tsx
    src/app/(main)/notes/[id]/page.tsx
  </files>
  <action>
Three changes:

**Login page (src/app/(auth)/login/page.tsx):**
1. Import Spinner from '@/components/ui/Spinner'
2. Replace "Loading..." text (line 109) with Spinner + "Signing in..."
3. Pattern: `{loading ? <><Spinner size="sm" className="mr-2" /> Signing in...</> : 'Login'}`

**Signup page (src/app/(auth)/signup/page.tsx):**
1. Same pattern as login - import Spinner
2. Replace loading text with Spinner + "Creating account..."

**Note editor page (src/app/(main)/notes/[id]/page.tsx):**
1. Add retry capability to the error state (lines 131-147)
2. Create local `refetchNote` function that re-runs the fetch logic
3. Add "Try Again" button next to "Back to Notes" button
4. Only show retry for fetch errors, not save/delete errors (check if `!note`)
5. Save/delete errors already auto-dismiss after 5 seconds - this is fine

For note editor, the refetch logic:
- Set loading true, error null
- Re-fetch note by ID
- Update state with result or error
  </action>
  <verify>
1. Login/signup: Click submit, see spinner while loading
2. Note editor: Visit invalid note ID, see retry button, click to retry fetch
  </verify>
  <done>Auth buttons show spinner during submission, note editor error shows retry option</done>
</task>

</tasks>

<verification>
1. `npm run lint` passes
2. `npm run build` passes
3. Manual verification:
   - Auth forms show spinner on submit
   - Home page error shows retry button
   - Note editor 404 shows retry button
</verification>

<success_criteria>
- POL-01: Loading indicators appear during async operations
  - Auth buttons show spinner during submission (added)
  - Notes skeleton during fetch (already exists in home page lines 30-44)
  - Note editor skeleton during fetch (already exists in lines 118-127)
- POL-02: Failed operations show user-friendly error with retry button
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish/03-01-SUMMARY.md`
</output>
