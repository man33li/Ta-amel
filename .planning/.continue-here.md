---
workflow: idle
phase: post-v3.1
status: smoke_test_passed_awaiting_merge
last_updated: 2026-04-30
---

<current_state>
v3.1 smoke test sections 1-9 confirmed green by the user on `feat/v3.0-local-first`. All recent fixes pushed to origin (HEAD = `af83d60`).

Recent commits ahead of `main`:
- `af83d60` Testing pages functionality (engines pin, globalThis singleton fix, locked-DB test, smoke-test doc)
- `04df435` fix(auth): handle locked DB cold-start and stale RSC cache after login
- `4f6a86b` fix(next): externalize SQLite native addons and surface unlockDb errors

Baselines: `tsc --noEmit` clean, `vitest run` 294/294 passing, working tree clean, branch up to date with `origin/feat/v3.0-local-first`.

Smoke-test §10 (production build + Docker) was re-verified locally on 2026-04-30 — `NEXT_TURBOPACK_EXPERIMENTAL_USE_SYSTEM_TLS_CERTS=1 npm run build` is the canonical incantation.
</current_state>

<completed_work>
- v1.0 (2026-01-27, `main`): cloud-backed, deployed to Vercel.
- v2.0 (2026-04-27, `feat/v2.0-palace-memory`): mem0ai memory + palace UX, still cloud-backed.
- v3.0 (2026-04-27, `feat/v3.0-local-first`): local-first stack, dropped Supabase/OpenAI/Vercel.
- v3.1 (2026-04-28, on `feat/v3.0-local-first`): all eight follow-ups including SQLCipher + rekey UI.
- 2026-04-29: cold-start auth fix + Next externalization fix + dev-mode globalThis singleton fix.
- 2026-04-30: smoke-test sections 1-9 walked through and confirmed green.
</completed_work>

<remaining_work>
Three discrete shippable steps left, all pending **explicit user authorization** per CLAUDE.md "Don't do these without explicit permission":

1. Merge `feat/v3.0-local-first` into `main`. Recommended shape: `--no-ff` so the merge commit marks "v3.1 landed".
2. Tag `v3.1` on `main` with annotated tag covering the local-first stack + SQLCipher + rekey + dev-mode singleton fix.
3. Push `main` and `v3.1` to `origin` — needs a fresh fine-grained PAT (Contents: Read+write, 1 hour). User must issue and revoke.

Optional before merge:
- Section §10.3 (Docker) of SMOKE-TEST.md — `docker build -t mindforge .` + container persistence test. Skipped today; can defer until after merge.
</remaining_work>

<decisions_made>
See `.planning/PROJECT.md` "Key Decisions" table — captures the v3.0/v3.1 rationale (drop Supabase, single-user passphrase, Xenova embedder, no graph/rerank, fresh start, SQLCipher, in-app rekey via bcrypt, rekey requires DELETE journal mode).

Newly decided 2026-04-29:
- `globalThis.__mindforge_db_cached` is load-bearing in dev mode (Turbopack instantiates a fresh module per route bundle on first compile). Don't refactor back to a module-local `let cached`.
</decisions_made>

<blockers>
- Push to `man33li/Ta-amel` requires user-supplied PAT; sandbox cannot authenticate against GitHub.
- Commit signing in this sandbox is environmental — only bypass with `-c commit.gpgsign=false` after explicit authorization.
</blockers>

<context>
Tech debt carried (non-blocking):
- TiptapEditor coverage at 67.74% (jsdom limitations).
- Note-edit page kept its own fetch loop instead of going through `useNotes` (debounce constraint).
- No direct test for the note-edit page itself (see TODO.md §1).

Out of scope:
- Multi-user / family share, device sync, mobile-optimized palace, v1/v2 data migration, subscription-based services.
</context>

<next_action>
Ask the user how they want to ship v3.1:
  a. Merge `feat/v3.0-local-first` -> `main` locally (no push), tag, then wait for PAT to push.
  b. Run §10.3 Docker check first.
  c. Defer merge; leave the branch live and capture next surfaced work.

Once they answer (a) and provide the PAT, the sequence is:
  git checkout main && git merge --no-ff feat/v3.0-local-first
  git tag -a v3.1 -m "v3.1 — local-first polish"
  git push origin main v3.1

Then update `.continue-here.md` to mark v3.1 shipped and prompt the user for the next idea.
</next_action>
