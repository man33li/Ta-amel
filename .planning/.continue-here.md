---
workflow: idle
phase: post-v3.1
status: v3.1_shipped
last_updated: 2026-04-30
---

<current_state>
v3.1 shipped 2026-04-30. `origin/main` and `origin/feat/v3.0-local-first` both at `12c5ca3`. Annotated tag `v3.1` published.

Tag points to: `12c5ca3 docs(planning): record v3.1 smoke-test pass + 2026-04-30 baseline`
Tag object: `af45008` (annotated, message documents the SQLCipher / rekey / cold-start fix payload).

Baselines: `tsc --noEmit` clean, `eslint .` clean, `vitest run` 294/294 passing, `NEXT_TURBOPACK_EXPERIMENTAL_USE_SYSTEM_TLS_CERTS=1 npm run build` clean (Next 16.1.4 / Turbopack, 22 dynamic routes). Working tree clean (until this very docs update lands).

Smoke-test §1-9 confirmed green by user 2026-04-30. §10 production build re-verified locally. §10.3 Docker check still deferred (Docker Desktop daemon was offline at the time).
</current_state>

<completed_work>
- v1.0 (2026-01-27, `main`): cloud-backed, deployed to Vercel.
- v2.0 (2026-04-27, `feat/v2.0-palace-memory`): mem0ai memory + palace UX, still cloud-backed.
- v3.0 (2026-04-27, `feat/v3.0-local-first`): local-first stack, dropped Supabase/OpenAI/Vercel.
- v3.1 (2026-04-28, on `feat/v3.0-local-first`): all eight follow-ups including SQLCipher + rekey UI.
- 2026-04-29: cold-start auth fix + Next externalization fix + dev-mode globalThis singleton fix.
- 2026-04-30: smoke-test §1-9 confirmed green, §10 build re-verified, FF-merged to `main`, tagged `v3.1`, pushed.
</completed_work>

<remaining_work>
None mandatory. Optional follow-ups, parked until usage demands them:

- §10.3 Docker check — `docker build -t mindforge .` + persistence test. Requires Docker Desktop daemon running. Static review noted that `wget` may not be in `node:22-bookworm-slim`; if the healthcheck flaps, add `RUN apt-get install -y --no-install-recommends wget` to the runner stage or swap to a `node -e require('http').get(...)` healthcheck.
- v3.1 GitHub release notes — promote the annotated tag (`af45008`) to a proper GitHub release page. `gh release create v3.1 --notes-from-tag` would do it once a PAT is issued for write-release scope.

Beyond that, see `.planning/TODO.md` for the deferred-features list. Pulling from "deferred" into active work goes via `/gsd-add-backlog` or `/gsd-new-milestone`, not by editing this file.
</remaining_work>

<decisions_made>
See `.planning/PROJECT.md` "Key Decisions" table — captures the v3.0/v3.1 rationale (drop Supabase, single-user passphrase, Xenova embedder, no graph/rerank, fresh start, SQLCipher, in-app rekey via bcrypt, rekey requires DELETE journal mode).

Newly decided 2026-04-29:
- `globalThis.__mindforge_db_cached` is load-bearing in dev mode (Turbopack instantiates a fresh module per route bundle on first compile). Don't refactor back to a module-local `let cached`.
</decisions_made>

<blockers>
None. Ready for next idea.

Pre-existing constraints (not blockers per se):
- Push to `man33li/Ta-amel` still requires a fresh user-supplied PAT each session; sandbox has no persisted credential.
- Commit signing in this sandbox is environmental — only bypass with `-c commit.gpgsign=false` after explicit authorization (current commits are unsigned).
</blockers>

<context>
Tech debt carried (non-blocking):
- TiptapEditor coverage at 67.74% (jsdom limitations).
- Note-edit page kept its own fetch loop instead of going through `useNotes` (debounce constraint).
- No direct test for the note-edit page itself (see TODO.md §1).

Out of scope:
- Multi-user / family share, device sync, mobile-optimized palace, v1/v2 data migration, subscription-based services.
</context>

<next_action>
v3.1 has shipped. Next session is whatever the user surfaces:

- Run §10.3 Docker check when daemon is up (informational; not blocking).
- Promote `v3.1` annotated tag to a GitHub Release (requires release-scope PAT).
- Plan v3.2 / v4 — new milestone goes through `/gsd-new-milestone` so PROJECT.md, TODO.md "Deferred features", and ROADMAP get updated together.
- Or just use the app for a while and let real usage surface the next priority — that was the v3.0/v3.1 thesis ("earn complexity with usage data").

If the next session opens with no specific request, prompt the user with the three options above and wait.
</next_action>
