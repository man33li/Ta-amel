---
workflow: idle
phase: post-v3.1
status: awaiting_user_local_run
last_updated: 2026-04-29
---

<current_state>
v3.1 polish pass shipped on `feat/v3.0-local-first`. Two post-merge fixes are committed locally but not yet pushed:
- `04df435` fix(auth): handle locked DB cold-start and stale RSC cache after login
- `4f6a86b` fix(next): externalize SQLite native addons and surface unlockDb errors

Baselines are green: `tsc --noEmit` clean, `eslint .` clean, `vitest run` 291/291 passing.

No active backlog. Awaiting the user's first real local run on `feat/v3.0-local-first` to surface follow-up work.
</current_state>

<completed_work>
- v1.0 (2026-01-27, `main`): cloud-backed, deployed to Vercel.
- v2.0 (2026-04-27, `feat/v2.0-palace-memory`): mem0ai memory + palace UX, still cloud-backed.
- v3.0 (2026-04-27, `feat/v3.0-local-first`): local-first stack, dropped Supabase/OpenAI/Vercel.
- v3.1 (2026-04-28, on `feat/v3.0-local-first`): all eight follow-ups including SQLCipher and rekey UI.
- 2026-04-29: cold-start auth fix + Next externalization fix (committed, not pushed).
</completed_work>

<remaining_work>
None defined. Next session is whatever surfaces from the user's first real local run.

Possible directions when the user is ready:
- Push the two pending fixes to `origin/feat/v3.0-local-first` and merge to `main`.
- Manual smoke test: setup → login → create cards → semantic search → lock → unlock → export → import → rekey.
- Cut a v3.1 tag once the merge lands.
</remaining_work>

<decisions_made>
See `.planning/PROJECT.md` "Key Decisions" table — captures the v3.0/v3.1 rationale (drop Supabase, single-user passphrase, Xenova embedder, no graph/rerank, fresh start, SQLCipher, in-app rekey via bcrypt, rekey requires DELETE journal mode).
</decisions_made>

<blockers>
- The two post-merge fixes have not been validated under real Next production runtime. Sandbox test pass does not equal "verified on the user's machine".
</blockers>

<context>
Tech debt carried (non-blocking):
- TiptapEditor coverage at 67.74% (jsdom limitations).
- Note-edit page kept its own fetch loop instead of going through `useNotes` (debounce constraint).

Out of scope:
- Multi-user / family share, device sync, mobile-optimized palace, v1/v2 data migration.
</context>

<next_action>
Wait for the user's local run. When they report results:
- If clean: prepare a v3.1 tag + push the local-first branch to `main`.
- If issues surface: route them through `/gsd-debug` or direct fix depending on scope.
</next_action>
